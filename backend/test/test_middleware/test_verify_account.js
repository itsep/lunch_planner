const { verifyAccount } = require('../../routes/middleware/verify_account')

describe('verify account', () => {
  it('TODO: SHOULD VERIFY ACCOUNT', () => {
    expect(verifyAccount(undefined, undefined, () => undefined)).equal(undefined)
  })
})
